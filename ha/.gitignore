# ─────────────────────────────────────────────────────────────
# 🔐 БЕЗОПАСНОСТЬ (Никогда не коммитьте!)
# ─────────────────────────────────────────────────────────────
secrets.yaml
config/secrets.yaml
*.secret
*.key
*.pem
*.crt
.password
.token
credentials.json
service_account.json

# ─────────────────────────────────────────────────────────────
# 🗄️ БАЗЫ ДАННЫХ
# ─────────────────────────────────────────────────────────────
# MariaDB/MySQL данные
mariadb/
mysql/
*.sql
*.sql.gz

# SQLite (если вернётесь на него)
config/home-assistant_v2.db
config/home-assistant_v2.db-*
*.db
*.db-wal
*.db-shm

# ─────────────────────────────────────────────────────────────
# 📝 ЛОГИ И ВРЕМЕННЫЕ ФАЙЛЫ
# ─────────────────────────────────────────────────────────────
config/home-assistant.log
config/home-assistant.log.*
*.log
logs/

# ─────────────────────────────────────────────────────────────
# 🐳 DOCKER
# ─────────────────────────────────────────────────────────────
# Исключения для docker-compose
!docker-compose.yml
!docker-compose.yaml
!*.yml
!*.yaml
*.yml.local
*.yaml.local

# Служебные файлы Docker
.DS_Store
Thumbs.db

# ─────────────────────────────────────────────────────────────
# 🏠 HOME ASSISTANT (Генерируемые файлы)
# ─────────────────────────────────────────────────────────────
config/.HA_VERSION
config/.ha_run
config/.storage
config/__pycache__/
config/*.pyc
config/deps/
# Кэш Python внутри custom_components
config/custom_components/__pycache__/
config/custom_components/*.pyc

# Кэши интеграций
config/.cloud
config/.config_entries.json

# Игнорируем только кэш внутри www (если есть)
config/www/__pycache__/
config/www/*.tmp


# ─────────────────────────────────────────────────────────────
# 📦 BACKUP И АРХИВЫ
# ─────────────────────────────────────────────────────────────
backups/
backup/
*.tar
*.tar.gz
*.zip
*.bak
*.backup

# ─────────────────────────────────────────────────────────────
# 💻 СИСТЕМНЫЕ ФАЙЛЫ
# ─────────────────────────────────────────────────────────────
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
